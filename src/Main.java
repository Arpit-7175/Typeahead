public class Main {
    public static void main(String[] args) {
        System.out.println("Hello world!");
        /*

            SuggestionController controller=new SuggestionController();
            methods in controller class
            1. add(String) -> adds a string to the trie and updates its frequency in hashmap
            2. addAll(List) -> adds multiple strings at once
            3. suggest(String) -> returns a List of words that might be a potentil search for the user.
            
         */
    }
}
